import { styled } from "@stitches/react";

export const HeaderContainer = styled("header", {
  width: "100%",
  padding: "1rem",
  backgroundColor: "#000",
  boxSizing: "border-box"
});

export const HeaderBrandName = styled("h1", {
  fontSize: "2rem",
  color: "#fff"
});